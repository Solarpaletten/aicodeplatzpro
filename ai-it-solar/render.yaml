services:
  # Backend API Service
  - type: web
    name: aicodeplatzpro-backend
    env: node
    plan: starter
    buildCommand: cd ai-it-solar/backend && npm install && npm run build
    startCommand: cd ai-it-solar/backend && npm run start:prod || npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: aicodeplatzpro-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://aicodeplatzpro.onrender.com
      - key: PORT
        value: 10000

  # Frontend Static Site
  - type: web
    name: aicodeplatzpro-frontend
    env: static
    buildCommand: cd ai-it-solar/frontend && npm install && npm run build
    staticPublishPath: ai-it-solar/frontend/dist
    envVars:
      - key: VITE_API_URL
        value: https://aicodeplatzpro-backend.onrender.com/api

# Database with new plan
databases:
  - name: aicodeplatzpro-db
    databaseName: ai_solar_production
    user: ai_solar_user
    plan: free
